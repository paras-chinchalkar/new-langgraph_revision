[project]
name = "agentic-ai-langraph"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain-tavily>=0.2.17",
    "langgraph-supervisor>=0.0.31",
    "langsmith>=0.7.3",
]
